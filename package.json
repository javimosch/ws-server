{
  "name": "ws-server-app",
  "version": "1.0.0",
  "description": "WebSocket server microservice for handling real-time communication",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "npx -y nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
